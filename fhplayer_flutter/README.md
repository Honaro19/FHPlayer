# FHPlayer Flutter App

This directory contains the shared Flutter application for FHPlayer.

The app is built from `fhplayer_flutter` and includes the Android,
iOS, web, Linux, macOS, and Windows Flutter targets.

For repository-level build and release guidance, see the root `README.md` and
`release/RELEASE_PROCESS.md`.
